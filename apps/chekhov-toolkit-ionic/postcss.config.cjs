// Intentionally empty. This file stops PostCSS's upward config search from
// reaching the repo-root postcss.config.js, which belongs to the React
// prototype (Tailwind) and is not installed for this Ionic app.
module.exports = {
  plugins: [],
};
