import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nmca.chekhovtoolkit',
  appName: 'chekhov-toolkit-ionic',
  webDir: 'dist'
};

export default config;
