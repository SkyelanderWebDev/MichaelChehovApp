# Build 0.1.0 — Active Secure Beta Lock

Date: 2026-07-02
Status: active secure beta
Build identifier: `secure-beta-0.1.0`
App version: `0.1.0`
Production alias: `https://michael-chekhov-toolkit-beta.vercel.app`

## Decision

Lock the Michael Chekhov Toolkit Ionic PWA as **Build 0.1.0** rather than `0.0.1`.

Reason: the app has crossed from internal/demo readiness into a real secure-beta lane. Lisa Dalton has the secure-beta production alias and created a tester account on 2026-07-02. The app already has receipt-backed Supabase Auth/Postgres/RLS, signed-in persistence, CI RLS verification, production security headers, and Dawson real-iPhone smoke receipts from 2026-07-01.

## Current stakeholder state

- Lisa Dalton is no longer only a future review target; she is an active secure-beta tester.
- Dawson already shared the secure-beta production alias with Lisa.
- Lisa created an account on 2026-07-02.
- Any future Lisa-facing build must be treated as an update to an active tester, not a pre-share demo.

## Build/version convention

Every merge intended for deployment, and every production deploy, must update the build identifier when tester-visible behavior, copy, auth/data shape, or security posture changes.

Keep these synchronized:

1. `apps/chekhov-toolkit-ionic/package.json` `version`
2. `apps/chekhov-toolkit-ionic/package-lock.json` root versions
3. `apps/chekhov-toolkit-ionic/src/constants/build.ts`
4. release/receipt note under `.claude/plans/`

CI now runs:

```bash
npm run verify:build-version
```

This checks that `package.json`, `package-lock.json`, `APP_VERSION`, and `BUILD_IDENTIFIER` agree. CI also runs `npm run verify:build-bump` to reject tester-facing Ionic changes without a build/release-note bump.

## Verification run for this lock

Run on 2026-07-02 after the test-harness fix:

```bash
cd apps/chekhov-toolkit-ionic
npm run lint
npm run build
npm run test:unit
npm run test:e2e:ci
npm run verify:build-version
```

Root reference gates also remain green:

```bash
npm run check
npm run build
```

## Non-negotiables still active

- No AI-generated embodied Michael Chekhov practice prompts.
- Keep taxonomy/source labels grounded in NMCA/Lisa-approved territory.
- Do not send Lisa a new build/update without Dawson approval.
- Do not deploy without gates and a release note.
- Preserve attribution visibility.
